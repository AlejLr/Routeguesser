/*
import { describe, it, expect, vi, beforeEach } from "vitest";
import * as MapScript from "./mapScript.js";
import Leaflet from "./leafletMock.js";

vi.mock("leaflet", () => Leaflet);

beforeEach(() => {
    // Reset mock implementations if needed
    vi.clearAllMocks();
});

describe("Testing MapScript.js Functions", () => {
    it("should start a new round", () => {
        const startNewRoundMock = vi.spyOn(MapScript, "startNewRound");
        MapScript.startNewRound();
        expect(startNewRoundMock).toHaveBeenCalled();
    });

    it("should clear the map correctly", () => {
        const clearMapMock = vi.spyOn(MapScript, "clearMap");
        MapScript.clearMap();
        expect(clearMapMock).toHaveBeenCalled();
    });

    it("should show neighbours correctly", () => {
        const showNeighboursMock = vi.spyOn(MapScript, "showNeighbours");
        MapScript.showNeighbours();
        expect(showNeighboursMock).toHaveBeenCalled();
    });
}); */
