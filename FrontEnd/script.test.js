import { describe, test, it, expect } from 'vitest';

// This unittest is using vitest, which was improted in the termial using "npm i -D vitest"
// The test is run by typing "npm test" in the terminal